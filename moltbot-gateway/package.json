{
  "name": "openclaw-gateway",
  "version": "1.0.0",
  "description": "OpenClaw Gateway Service for Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install -g openclaw@latest && openclaw onboard --install-daemon"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
{
  "name": "moltbot-gateway",
  "version": "1.0.0",
  "description": "Moltbot Gateway Service for Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install -g moltbot && moltbot onboard --install-daemon"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}